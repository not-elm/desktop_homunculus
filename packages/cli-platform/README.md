# @hmcs/cli platform packages

Platform-specific binary packages for [@hmcs/cli](https://www.npmjs.com/package/@hmcs/cli).

Do not install these directly. Install `@hmcs/cli` instead:

```bash
npm install -g @hmcs/cli
```
