# Unify "MOD commands" Naming

**Date:** 2026-03-17
**Status:** Approved

## Problem

The concept of on-demand scripts exposed by MODs via `"bin"` in `package.json` has no consistent name. Documentation and code comments use multiple terms interchangeably:

- "bin command(s)"
- "on-demand command(s)"
- "on-demand script(s)"
- "bin script(s)"

This inconsistency makes documentation harder to follow and search.

## Decision

Unify all natural-language references to **"MOD command(s)"** (with "MOD" in uppercase, matching existing project convention).

## Scope

### In scope

- Documentation text in `docs/website/` (English and Japanese)
- Project documentation (root `CLAUDE.md`, `CLAUDE.md`)
- SDK JSDoc comments (`packages/sdk/src/`)
- Rust doc comments (`engine/crates/`)
- File rename: `bin-commands.md` → `commands.md` (English + Japanese)
- Internal Markdown links and anchors referencing the renamed file
- Auto-generated files that derive from updated sources (OpenAPI spec, API reference docs)

### Out of scope

- Code identifiers: `executeCommand`, `CommandResult`, `ExecuteCommandRequest`, `CommandEvent`, etc.
- SDK module name: `@hmcs/sdk/commands`
- HTTP API paths: `/commands/execute`
- MCP tool name: `execute_command`
- package.json field: `"bin"`
- Docusaurus URL redirects (not requested)

## Replacement Rules

### English

| Current term | Replacement |
|---|---|
| "bin command(s)" | "MOD command(s)" |
| "on-demand command(s)" | "MOD command(s)" |
| "on-demand script(s)" | "MOD command(s)" |
| "bin script(s)" | "MOD command(s)" |

Apply case-matching to headings (e.g., "Bin Commands" → "MOD Commands"). Note that "MOD" is always uppercase per project convention.

### Japanese

| Current term | Replacement |
|---|---|
| "bin コマンド" / "Bin コマンド" | "MOD コマンド" |
| "bin スクリプト" | "MOD コマンド" |
| "オンデマンドコマンド" | "MOD コマンド" |
| "オンデマンドスクリプト" | "MOD コマンド" |

### Exceptions

- "on-demand" may still be used as a supplementary descriptor when contrasting with services (e.g., "MOD commands are on-demand scripts that run when invoked, unlike services which run continuously.")
- Text inside code blocks, API examples, and command-line examples is not modified.
- Code identifiers are not modified.

## File Rename

- `docs/website/docs/mod-development/bin-commands.md` → `commands.md`
- `docs/website/i18n/ja/docusaurus-plugin-content-docs/current/mod-development/bin-commands.md` → `commands.md`
- Update all internal Markdown links from `bin-commands` to `commands`
- Docusaurus sidebar uses `autogenerated` mode, so no sidebar config change is needed

### Anchor Handling

Section headings renamed from "Bin Commands" to "MOD Commands" will change auto-generated anchors (e.g., `#bin-commands` → `#mod-commands`). All internal references to `#bin-commands` anchors must be updated. Where Japanese docs use explicit `{#bin-commands}` anchor IDs, update them to `{#mod-commands}`.

## Build Steps

After updating Rust doc comments in `engine/crates/homunculus_utils/src/schema/mods.rs`:

1. Run `make gen-open-api` to regenerate `docs/website/static/api/open-api.yml`
2. The Docusaurus OpenAPI plugin will regenerate API reference docs (`*.api.mdx`, `*.StatusCodes.json`) from the updated YAML on next build

## Affected Files

The complete set of affected files should be discovered by searching for the replacement target terms. The lists below are representative, not exhaustive — the implementer should run a systematic search to catch all occurrences.

### English documentation (`docs/website/docs/`)

- `mod-development/bin-commands.md` (rename to `commands.md` + content)
- `mod-development/project-setup/package-json.md`
- `mod-development/project-setup/directory-structure.md`
- `mod-development/sdk/commands/index.md`
- `mod-development/sdk/commands/output-succeed.md`
- `mod-development/sdk/index.md`
- `mod-development/sdk/quick-start.md`
- `mod-development/sdk/mods/executeCommand.md`
- `mod-development/sdk/mods/index.md`
- `mod-development/sdk/mods/list.md`
- `mod-development/sdk/mods/types.md`
- `mod-development/sdk/app/types.md`
- `mod-development/sdk/signals/index.md`
- `mod-development/menus.md`
- `mod-development/tray-menus.md`
- `mod-development/webview-ui/setup-and-build.md`
- `mod-development/publishing.md`
- `mod-development/index.md`
- `mods/voicevox.md` (link text + link target)
- `mods/menu.md`
- `reference/mcp-tools/resources.md`
- `reference/mcp-tools/mod.md`

### Japanese documentation (`docs/website/i18n/ja/`)

Japanese equivalents of all files listed above.

### Project documentation

- `CLAUDE.md` (desktop_homunculus root)

### SDK comments

- `packages/sdk/src/commands.ts` (JSDoc)
- `packages/sdk/src/mods.ts` (JSDoc)
- `packages/sdk/src/app.ts` (JSDoc)

### Rust comments

- `engine/crates/homunculus_utils/src/schema/mods.rs` (doc comments — triggers OpenAPI regeneration)
- `engine/crates/homunculus_mod/src/lib.rs` (module doc comment)

### Auto-generated (updated via build steps, not manually)

- `docs/website/static/api/open-api.yml`
- `docs/website/docs/reference/api/*.api.mdx` and `*.StatusCodes.json`
