# Sandbox

This package is a local sandbox for workspace-level mod composition.

## Purpose

- Aggregate official `@hmcs/*` mod packages in one place.
- Keep local development and manual testing dependencies explicit.
- Avoid publishing: this package is private and intended for in-repo use only.

